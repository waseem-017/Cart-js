import { Fragment } from 'react';
import AvailableProducts from './AvailableProducts';

const Products = () => {
  return (
    <Fragment>
      <AvailableProducts />
    </Fragment>
  );
};

export default Products;
